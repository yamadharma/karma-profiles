USE="\
mmx mmx2 mmxext sse sse2 \
ssse3 hvm \
"

CFLAGS="-march=nocona -mtune=nocona -O2 -pipe -pipe -fforce-addr -falign-functions=4 -fprefetch-loop-arrays -fomit-frame-pointer"
CXXFLAGS="${CFLAGS}"
